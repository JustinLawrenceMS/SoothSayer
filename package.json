{
  "name": "soothsayer",
  "version": "1.0.0",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "export SET NODE_OPTIONS=--openssl-legacy-provider && npx expo start",
    "android": "export SET NODE_OPTIONS=--openssl-legacy-provider && expo start --android",
    "ios": "export SET NODE_OPTIONS=--openssl-legacy-provider && expo start --ios",
    "web": "export SET NODE_OPTIONS=--openssl-legacy-provider && npx expo start --web"
  },
  "dependencies": {
    "@expo/webpack-config": "^19.0.1",
    "expo": "^50.0.21",
    "expo-font": "^12.0.10",
    "expo-splash-screen": "^0.27.6",
    "expo-status-bar": "^1.12.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-native": "^0.75.4",
    "react-native-web": "^0.19.12"
  },
  "devDependencies": {
    "@babel/core": "^7.25.7"
  },
  "private": true
}
